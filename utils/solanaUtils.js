// Funciones auxiliares para interactuar con Solana
const extractDeviceData = (accountInfo) => {
    // Lógica para extraer los datos del dispositivo de accountInfo
};

module.exports = { extractDeviceData };